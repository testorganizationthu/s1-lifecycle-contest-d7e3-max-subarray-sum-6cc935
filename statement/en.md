{{ self.title() }}

{{ s('description') }}
Given an array of N integers (-10^9 <= a_i <= 10^9), find the contiguous subarray with the largest sum. You must select at least one element.

{{ s('input format') }}
First line: N (1 <= N <= 200000). Second line: N integers.

{{ s('output format') }}
Single integer.

{{ s('sample', 1) }}
{{ self.sample_text() }}
